import { Canvas, Meta, Story } from '@storybook/addon-docs';
import DropDownMenu from './DropDownMenu';
import Menu from '../Menu';
import Icon from '../Icon/Icon';


<Meta title="Components/DropDownMenu" component={DropDownMenu}/>

# DropDownMenu

DropDownMenu display a list of choices on temporary surfaces.

<Canvas>
  <DropDownMenu
    contentRender={(close) => (
      <Menu>
          <Menu.Item prefix={<Icon name="BsFillGearFill" />} suffix={<Icon name="BsFillGearFill" />}>Settings</Menu.Item>
          <Menu.Item prefix={<Icon name="BsPeopleFill" />}>Profile</Menu.Item>
          <Menu.Item isHeading></Menu.Item>
          <Menu.Item prefix={<Icon name="BsPower" />}>Logout</Menu.Item>
      </Menu>
    )}
  >
    Menu
  </DropDownMenu>
</Canvas>

## Properties

|Property|Type|Default Value|Description|Required|
|---|---|---|---|---|
|items|IDropdownItemType|-|The items to display in the menu.|Yes|
|offsetX|number|0|The horizontal offset of the menu.|No|
|offsetY|number|0|The vertical offset of the menu.|No|
|closeOnClick|boolean|true|Close when click inside floating menu.|No|
|dropDownClassName|string|undefined|The class name to apply to the dropdown.|No|
|chevron|boolean|true|Show dropdown chevron.|No|
|children|ReactNode|-|The content to display in the menu.|No|
|contentRender|function(close: Function) => React.ReactNode|-|Render Contents of function|No|
|testingID| string | undefined | The id used for testing purposes.<br/>`<div data-testid="my-test-id"/>` |No|
