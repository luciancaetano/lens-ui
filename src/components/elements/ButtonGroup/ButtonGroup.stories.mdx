import { Canvas, Meta, Story } from '@storybook/addon-docs';
import ButtonGroup from './ButtonGroup';
import Button from '../Button/Button';
import LensProvider from '../../providers/LensProvider/LensProvider';
import Icon from '../Icon/Icon';

<Meta title="Components/ButtonGroup" component={ButtonGroup}  decorators={[(Story) => <LensProvider>{Story()}</LensProvider>]}/>


# ButtonGroup

The ButtonGroup component can be used to group related buttons.

<Canvas>
    <ButtonGroup>
        <Button>
            Button 1
        </Button>
        <Button>
            Button 2
        </Button>
        <Button>
            Button 3
        </Button>
    </ButtonGroup>
</Canvas>

### Properties

|Property|Type|Default Value|Description|Required|
|---|---|---|---|---|
|vertical|boolean|false| Whether the button group should be vertical.|No|
|testingID| string | undefined | The id used for testing purposes.<br/>`<div data-testid="my-test-id"/>` |No|

---

### Horizontal

<Canvas>
    <ButtonGroup vertical={false}>
        <Button>
            Button 1
        </Button>
        <Button>
            Button 2
        </Button>
        <Button>
            Button 3
        </Button>
    </ButtonGroup>
</Canvas>

---

### Vertical
<Canvas>
    <ButtonGroup vertical>
        <Button>
            Button 1
        </Button>
        <Button>
            Button 2
        </Button>
        <Button>
            Button 3
        </Button>
    </ButtonGroup>
</Canvas>